---
title: "3. A Complete Haskell Program"
description: "Assemble known pieces and understand comments, signatures, main, IO (), do, indentation, and execution order."
---

You already know strings, function application, `putStrLn`, and `print`. We can now assemble those pieces.

```haskell
-- PURPOSE: Calculate a cube, then display a label and the result.
-- Two hyphens start a comment. Haskell ignores the rest of this line.

-- TYPE: cube accepts one Integer and returns one Integer.
cube :: Integer -> Integer

-- PROCESS: Multiply the input x by itself three times.
cube x = x * x * x

-- ENTRY POINT: main is the action the runtime starts.
main :: IO ()
main = do
  -- OUTPUT 1: Write this String, then move to a new line.
  putStrLn "The cube of 4 is:"

  -- PROCESS THEN OUTPUT 2: Calculate cube 4, then display 64.
  print (cube 4)
```

Exact output:

```text
The cube of 4 is:
64
```

## Every component

| Piece | Read aloud | Meaning |
|---|---|---|
| `--` | comment | Ignore the rest of this line during compilation |
| `cube` | cube | A programmer-chosen function name |
| `::` | has type | Introduces a type signature |
| `Integer -> Integer` | Integer to Integer | One whole-number input gives one whole-number result |
| `=` | is defined as | Separates a definition from its meaning |
| `main` | main | The program's starting action |
| `IO ()` | I-O unit | An external-world action with no useful returned data |
| `do` | do | Begins a readable action sequence |
| `(cube 4)` | cube of four | The pure expression supplied to `print` |

<Mermaid chart={`flowchart TD
  subgraph Definitions["Top level, outside main"]
    S["cube :: Integer -> Integer"] --> D["cube x = x * x * x"]
  end
  subgraph Main["main's do block"]
    M1["putStrLn \"The cube of 4 is:\""] --> M2["print (cube 4)"]
  end
  Definitions -. "cube is available wherever it is called" .-> M2`} caption="A type signature and a definition describe cube once; main's do block is a separate, ordered list of actions that may call it." />

## Value versus action

<ComparisonTable
  columns={[
    { header: 'Expression', key: 'expression' },
    { header: 'Kind', key: 'kind' },
    { header: 'Result', key: 'result' }
  ]}
  rows={[
    { expression: 'cube 4', kind: 'Pure expression', result: 'Evaluates to 64 without printing' },
    { expression: 'print (cube 4)', kind: 'IO action', result: 'Calculates 64, displays it, and adds a newline' }
  ]}
/>

`IO` marks a description of work involving the external world. `()` is **unit**, a value carrying no useful information. The useful outcome of terminal output is the visible effect.

<Quiz title="Check: assembling the pieces" questions={[
  {
    question: "cube 4 appears inside print (cube 4). Which statement correctly separates what is a value from what is an action here?",
    options: [
      "cube 4 is itself an IO action, because it appears inside main's do block",
      "cube 4 is a pure Integer-producing expression; print (cube 4) is the IO () action that displays whatever cube 4 produces",
      "print (cube 4) is a pure expression because cube is pure",
      "Both cube 4 and print (cube 4) are IO actions, since both appear inside a do block"
    ],
    correctIndex: 1,
    explanation: "cube :: Integer -> Integer is an entirely pure function — cube 4 is just a number, 64, with no notion of terminal output. Wrapping it in print is what turns that number into an IO () action capable of producing a visible effect. Being written inside a do block does not make a subexpression an action; only print (cube 4) as a whole is the action listed in that do block.",
    optionFeedback: [
      "cube 4 by itself has type Integer, not IO anything — it is purely a calculation with no interaction with the outside world.",
      "",
      "print (cube 4) has type IO (), which marks it as an action, precisely because print turns a pure value into something that can affect the terminal.",
      "Only print (cube 4) is the IO action; cube 4 is the pure Integer value passed into it. The do block lists actions, and cube 4 alone is not one of its listed lines."
    ]
  }
]} />

## `do` and execution order

The `do` block lists multiple actions in order. Both action lines are aligned, so Haskell groups them into the same block.

<InteractiveStepper>
  <StepperStep title="Start">The runtime begins at `main`.</StepperStep>
  <StepperStep title="First action">`putStrLn` writes the label and newline.</StepperStep>
  <StepperStep title="Pure processing">`cube 4` evaluates to `64`.</StepperStep>
  <StepperStep title="Second action">`print` displays `64` and a newline.</StepperStep>
  <StepperStep title="Finish">The action sequence finishes with unit `()`.</StepperStep>
</InteractiveStepper>

<Callout type="info" title="This is action order, not evaluation order">
The steps above describe the order the two **IO actions** run in: `putStrLn` first, then `print`. They do not mean Haskell evaluates every expression strictly, top to bottom, the moment execution reaches it. Haskell is a **lazy** language — a pure expression like `cube 4` is only evaluated once its value is actually needed, which here happens to be while `print` is performing its action. For this particular program the difference is invisible, because `cube 4` has no side effects to delay. Treat the sequence above as "this is the order the *actions* run in," not "this is the order everything is *computed* in" — Chapter 10 covers lazy evaluation properly.
</Callout>

<Quiz title="Check: do-block ordering" questions={[
  {
    question: "The InteractiveStepper above lists \"Pure processing: cube 4 evaluates to 64\" as a step between the two IO actions. What does the callout say about taking that literally as strict, top-to-bottom evaluation?",
    options: [
      "It confirms that is exactly correct: Haskell evaluates every expression the instant execution reaches that line",
      "It warns that the steps describe the order IO actions run in, not a claim that every expression is evaluated strictly at that point — Haskell is lazy and only evaluates cube 4 when its value is actually needed",
      "It says the stepper is completely wrong and should be ignored",
      "It says cube 4 is evaluated before the program starts, at compile time"
    ],
    correctIndex: 1,
    explanation: "The stepper is a useful simplification for understanding which IO actions happen in which order. But Haskell does not evaluate expressions strictly the instant control reaches them; it evaluates lazily, only when a value is actually demanded. For this simple program the visible behaviour is identical either way, but the mental model \"Haskell runs top to bottom evaluating as it goes\" is the wrong one to carry forward.",
    optionFeedback: [
      "This is precisely the misconception the callout corrects — Haskell does not evaluate strictly top to bottom.",
      "",
      "The stepper is not wrong as a description of IO action order; it is only incomplete as a description of evaluation order, which the callout clarifies.",
      "cube 4 depends on nothing but a literal, but in general Haskell values are computed lazily at run time when demanded, not ahead of time at compile time."
    ]
  }
]} />

## Save, load, run

Save the source as `FirstProgram.hs`:

```text
ghci> :load FirstProgram.hs
Ok, one module loaded.
ghci> main
The cube of 4 is:
64
```

Compiler progress wording can vary. The successful load and exact two output lines are the important evidence.

## Common failures

| Failure | Cause | Repair |
|---|---|---|
| Output lines are not aligned | The layout no longer describes one clean sequence | Align actions beneath `do` |
| `main =` has nothing after it | Every definition needs a right side | Supply one action or a `do` block |
| `cube :: Integer` | This describes a value, not a function | Use `Integer -> Integer` |
| `print cube 4` | Application groups left incorrectly for this intention | Use `print (cube 4)` |

<Checklist title="Mastery check" rules={[
  { type: 'must', text: 'I can name every symbol in a small complete program: comments, signature, main, IO (), do, indentation.' },
  { type: 'must', text: 'I can distinguish a pure expression like cube 4 from an IO action like print (cube 4).' },
  { type: 'must', text: 'I can state the order in which the actions inside a do block run.' },
  { type: 'must', text: 'I can explain why "action order" is not the same claim as "strict evaluation order".' },
  { type: 'avoid', text: 'Do not misalign the lines of a do block; layout is meaningful in Haskell, not cosmetic.' },
  { type: 'note', text: 'Haskell is lazy: a pure expression is evaluated when its value is needed, not necessarily the instant it is written. Chapter 10 covers this in full.' }
]} />
