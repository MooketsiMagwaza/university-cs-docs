---
title: "3. Tuples"
description: "Group values of different types with fixed arity, use fst and snd and tuple patterns, return several results from one function, and choose between a list and a tuple."
---



A **tuple** is a finite sequence of values of **possibly different types**. Components are separated by commas and enclosed in round brackets.

```haskell
(50, "csi243", "2013/14")
('2', False)
```

Where a list is homogeneous and variable in length, a tuple is heterogeneous and fixed. That single contrast decides which one a problem needs.

## 1. Arity

**Arity** is the number of components in a tuple, and it is part of the type.

| Tuple | Arity | Name |
|---|---|---|
| `()` | 0 | Unit |
| `(1, 'a')` | 2 | Pair |
| `(1, 'a', True)` | 3 | Triple |
| `(1, 2, 3, 4)` | 4 | 4-tuple |

Two rules follow:

- **Tuples of arity 1 are not allowed.** `(5)` is just `5` in brackets, not a tuple.
- **The unit `()`** is the empty tuple. You have already met it in `IO ()`, where it means the action returns no useful data.

<Quiz title="Check: Arity" questions={[
  {
    question: "What is the arity of (1, 'a', True, \"go\")?",
    options: ["2", "3", "4", "1"],
    correctIndex: 2,
    explanation: "Arity is simply a count of the components, regardless of their types. This tuple has four components — 1, 'a', True, \"go\" — so its arity is 4, and its type is (Integer, Char, Bool, String).",
    optionFeedback: [
      "That's the arity of a pair — this tuple has more components than that.",
      "That's the arity of a triple — count again, there's one more component here.",
      "Correct — four components, one of each: Integer, Char, Bool, String.",
      "That would make this a singleton, but tuples of arity 1 don't exist — and this tuple clearly has more than one component anyway."
    ]
  },
  {
    question: "Why isn't (5) a tuple of arity 1?",
    options: [
      "Because Haskell tuples must contain at least two components, and (5) is just 5 written with brackets around it",
      "Because 5 is not a valid tuple element",
      "Because arity-1 tuples exist but must use square brackets instead",
      "Because (5) is a syntax error"
    ],
    correctIndex: 0,
    explanation: "Round brackets around a single expression are just grouping, the same as in ordinary arithmetic — (5) means exactly 5. Haskell's tuple types only start at arity 2 (pairs); there is no built-in one-component tuple.",
    optionFeedback: [
      "Correct — round brackets around one value are just parentheses for grouping, not tuple syntax; tuples start at arity 2.",
      "5 can absolutely appear inside a real tuple, e.g. (5, 'a') — the issue is that a single value alone isn't a tuple at all.",
      "Square brackets denote a list, not a tuple, and a list of arity 1, i.e. [5], is a perfectly normal singleton list — a different thing from a one-component tuple, which doesn't exist either way.",
      "(5) compiles just fine — it's simply equal to the Integer 5, not a tuple."
    ]
  }
]} />

## 2. The type records every position

A tuple's type lists the type of each position, in order:

```haskell
student :: (String, Integer, Bool)
student = ("Kago", 2026001, True)
```

This means `(String, Integer)` and `(Integer, String)` are **different types**, and neither can be used where the other is expected. Position carries meaning.

```text
ghci> :type ('2', False)
('2', False) :: (Char, Bool)
ghci> :type (50, "csi243", "2013/14")
(50, "csi243", "2013/14") :: Num a => (a, String, String)
```

<Quiz title="Check: Type Records Position" questions={[
  {
    question: "Are (String, Integer) and (Integer, String) the same type?",
    options: [
      "Yes — a tuple's type is just the set of types it contains, regardless of order",
      "No — position is part of the type, so swapping the order gives a different type entirely",
      "Yes, but only when both components happen to hold the same value",
      "They're different types only when the values are different"
    ],
    correctIndex: 1,
    explanation: "A tuple type lists the type of each position in order. (\"Kago\", 5) :: (String, Integer) cannot be used where (Integer, String) is expected — GHC checks position by position, and position 1 is String in one type but Integer in the other.",
    optionFeedback: [
      "Order matters for tuples — this is exactly what makes them different from an unordered collection of types.",
      "Correct — a tuple's type names the type at every position, in order, so reordering the positions changes the type.",
      "This is about the types themselves, not the runtime values inside a particular tuple — the types differ regardless of what values are ever put in them.",
      "It's the other way around: the types differ because of position, independent of whatever specific values happen to be used."
    ]
  },
  {
    question: "What would `student :: (String, Integer, Bool)` reject as an argument?",
    options: [
      "(\"Kago\", 2026001, True)",
      "(\"Kago\", True, 2026001) — same three values, different order",
      "Both are accepted; order doesn't matter as long as the types are all present",
      "Neither is accepted; the type signature is invalid"
    ],
    correctIndex: 1,
    explanation: "The type fixes String first, then Integer, then Bool. Swapping the Integer and Bool positions produces a value of type (String, Bool, Integer) — a different type from (String, Integer, Bool) — so it is rejected.",
    optionFeedback: [
      "This matches the declared type position for position — String, then Integer, then Bool — so it's accepted, not rejected.",
      "Correct — swapping Integer and Bool changes the type to (String, Bool, Integer), which doesn't match the (String, Integer, Bool) signature.",
      "Order matters a great deal for tuple types — that's the whole point of this section.",
      "The first option is a perfectly valid (String, Integer, Bool), matching the signature exactly."
    ]
  }
]} />

## 3. Tuples are immutable and fixed

The elements in a tuple are fixed. You cannot add elements to a tuple as the program runs, and there is no cons operator for tuples.

Use a tuple when you know **in advance** how many values are to be stored, and a list when the count can vary.

<Callout type="warn" title="Arity cannot change">
Adding a component produces a value of a *different type*, not a longer tuple. A function returning `(Int, Int)` can never return `(Int, Int, Int)`.
</Callout>

<Quiz title="Check: Immutable and Fixed" questions={[
  {
    question: "A function needs to accumulate an unknown, growing number of student records as the program runs. Should it use a list or a tuple?",
    options: [
      "A tuple, since it groups related data together",
      "A list, since the count of records isn't known in advance and can change",
      "Either works identically here",
      "Neither — this requires a different data structure entirely"
    ],
    correctIndex: 1,
    explanation: "Tuples have arity fixed by their type — you cannot grow a (String, Integer) into a (String, Integer, String, Integer) at run time. Whenever the count of things can vary, that's the list use case, even if each individual thing (like one record) is itself represented as a tuple.",
    optionFeedback: [
      "Grouping related data is what a tuple is good at for a single, fixed-shape record — but here the number of records itself varies, which tuples can't express.",
      "Correct — an unknown, changeable count of items is exactly what a list is for; each item could still be a tuple internally.",
      "They behave very differently here: a tuple's arity is baked into its type and cannot grow, while a list's length is free to change.",
      "A list of tuples — [(String, Integer)] — handles this perfectly well; no new structure is needed."
    ]
  },
  {
    question: "Why is 'a function returning (Int, Int) can never return (Int, Int, Int)' true?",
    options: [
      "Because Haskell functions can only return two values at most",
      "Because (Int, Int) and (Int, Int, Int) are different types, and a function's return type is fixed by its signature",
      "Because Int can only appear twice in any single tuple",
      "It isn't true — a function can return either depending on the input"
    ],
    correctIndex: 1,
    explanation: "A function's signature commits to one return type. (Int, Int) and (Int, Int, Int) are simply different types — arity is baked into the type itself — so a single function cannot produce values of both types depending on circumstance.",
    optionFeedback: [
      "There's no cap on tuple arity — 4-tuples, 5-tuples and beyond are all valid; the real constraint is that one function commits to one fixed return type.",
      "Correct — the two tuple shapes are different types, and a function's declared return type cannot vary between calls.",
      "Int can appear any number of times across different tuple types — (Int,Int,Int,Int) is perfectly legal on its own.",
      "A function's return type is fixed once and for all by its signature — it can't switch shapes based on the input it receives."
    ]
  }
]} />

## 4. `fst` and `snd`

Most tuples are pairs, so the Prelude provides two functions for them:

```text
ghci> fst (5, 'h')
5
ghci> snd (5, 'h')
'h'
ghci> fst ("Tebogo", "Lesego")
"Tebogo"
```

They are defined with tuple patterns, exactly as Chapter 4 showed:

```haskell
fst :: (a, b) -> a
fst (x, _) = x

snd :: (a, b) -> b
snd (_, x) = x
```

<Callout type="warn" title="Pairs only">
`fst` and `snd` work only on tuples of arity 2. For a triple, use a pattern: `(a, _, _)`.
</Callout>

<Quiz title="Check: fst and snd" questions={[
  {
    question: "What does fst (1, 'a', True) do?",
    options: [
      "Returns 1",
      "Returns ('a', True)",
      "Fails to compile — fst only accepts a pair (arity 2), and this is a triple",
      "Returns 'a', the second component"
    ],
    correctIndex: 2,
    explanation: "fst :: (a, b) -> a is defined only for pairs. A triple (1, 'a', True) :: (Integer, Char, Bool) doesn't match the pattern fst expects, so GHC reports a type error rather than guessing which component you meant.",
    optionFeedback: [
      "This is what fst would return if it worked on this tuple — but it doesn't, because fst is defined only for pairs.",
      "This isn't what fst returns even for a pair; and more fundamentally, fst rejects a triple entirely.",
      "Correct — fst's type, (a, b) -> a, only matches tuples of arity 2. A triple simply doesn't fit that pattern.",
      "fst never returns the second component of anything — that would be snd's job, and even snd only works on pairs."
    ]
  },
  {
    question: "Given fst (x, _) = x, what does the _ in the pattern signify?",
    options: [
      "That the second component must be an underscore character",
      "That the second component is matched but its value is discarded — it's never used on the right-hand side",
      "That fst only works when the second component is missing",
      "A syntax error in the definition"
    ],
    correctIndex: 1,
    explanation: "_ is a wildcard pattern: it matches anything, but binds no name, signaling 'this part of the value doesn't matter for what follows.' fst's whole point is to extract just the first component, so the second is matched (to satisfy the tuple's shape) and immediately ignored.",
    optionFeedback: [
      "_ is a wildcard in a pattern, not a literal underscore character to match against — it matches any value at all.",
      "Correct — the tuple must still have two components for the pattern to match, but the second one is simply unused inside fst's definition.",
      "The pair still needs a real second component — (5, 'h') for instance; the wildcard just means fst doesn't care what it is.",
      "This is standard, valid Haskell pattern syntax — it's exactly how fst and snd are defined in the Prelude."
    ]
  }
]} />

## 5. Tuple patterns

Destructuring is usually clearer than repeated `fst`/`snd` calls:

```haskell
-- Add the components of a pair.
addPair :: (Integer, Integer) -> Integer
addPair (x, y) = x + y

-- Swap the components of a pair.
swap' :: (a, b) -> (b, a)
swap' (x, y) = (y, x)

-- Take the middle of a triple.
middle :: (a, b, c) -> b
middle (_, y, _) = y
```

```text
ghci> addPair (3, 4)
7
ghci> swap' (1, "one")
("one",1)
```

A complete program runs all three functions on a couple of different tuples each:

```haskell
main :: IO ()
main = do
  print (addPair (3, 4))
  print (addPair (10, -2))
  print (swap' (1, "one"))
  print (swap' ("Kago", True))
  print (middle (1, 2, 3))
  print (middle ('a', 'b', 'c'))
```

Exact output:

```text
7
8
("one",1)
(True,"Kago")
2
'b'
```

`swap'` is polymorphic in both positions — it works the same way whether the pair holds `(Integer, String)` or `(String, Bool)`, because `swap' (x, y) = (y, x)` never inspects what `x` or `y` actually are.

Patterns nest, so a tuple inside a tuple can be taken apart in one step:

```text
ghci> let nested = ((1, 'a'), "foo")
ghci> snd (fst nested)
'a'
```

<Mermaid chart={`flowchart TD
  N["nested"] --> A["(1, 'a')"]
  N --> B["the String foo"]
  A --> A1["1"]
  A --> A2["'a'"]
`} caption="nested = ((1,'a'), 'foo') as a small tree. fst reaches the left branch, snd the right; snd (fst nested) walks left then right." />

`fst nested` walks to the left branch, `(1, 'a')`; `snd` of that walks to its right branch, `'a'` — reading the diagram top to bottom traces exactly the calls made.

Or with a single pattern:

```haskell
innerChar :: ((Int, Char), String) -> Char
innerChar ((_, c), _) = c
```

<Quiz title="Check: Tuple Patterns" questions={[
  {
    question: "Given middle (_, y, _) = y, what does middle (1, 2, 3) return?",
    options: ["1", "2", "3", "(1, 2, 3)"],
    correctIndex: 1,
    explanation: "The pattern (_, y, _) binds the middle component to y and discards the first and third with wildcards. For (1, 2, 3), the middle position holds 2, so middle (1, 2, 3) is 2.",
    optionFeedback: [
      "1 is the first component, matched by the first _ and discarded — not what middle returns.",
      "Correct — y binds to the middle position, which holds 2 in this tuple.",
      "3 is the third component, matched by the second _ and discarded — not what middle returns.",
      "middle returns just the one component bound to y, not the whole tuple."
    ]
  },
  {
    question: "For nested = ((1, 'a'), \"foo\"), what does fst nested return?",
    options: [
      "1",
      "(1, 'a')",
      "\"foo\"",
      "'a'"
    ],
    correctIndex: 1,
    explanation: "nested is a pair: its first component is the whole inner pair (1, 'a'), and its second component is \"foo\". fst nested returns that first component as-is, without looking inside it — reaching into (1, 'a') itself needs a further fst or snd.",
    optionFeedback: [
      "1 is nested one level deeper — reaching it needs fst (fst nested), not fst nested alone.",
      "Correct — nested's outer first component is the whole pair (1, 'a'); fst stops there without unpacking it further.",
      "\"foo\" is nested's second component, so this is what snd nested would return, not fst nested.",
      "'a' is two levels deep — snd (fst nested) is needed to reach it, as the example above shows."
    ]
  }
]} />

## 6. Nesting

There are no restrictions on the types of a tuple's components. They may be tuples, lists, or anything else:

```haskell
(50, ("cs", False))                  -- tuple of tuples
(['2', 'a', 'x'], [False, False])    -- tuple of lists
[("cs", False), ("sc", True)]        -- list of tuples
```

The last is the important one. **A list of tuples** is how Haskell represents a table before custom types are available: every row has the same shape, and there can be any number of rows.

```haskell
marks :: [(String, Integer)]
marks = [("Kago", 72), ("Naledi", 68), ("Thato", 91)]
```

```text
ghci> length marks
3
ghci> fst (head marks)
"Kago"
ghci> map snd marks
[72,68,91]
```

The last line uses `map` to apply `snd` to every pair in `marks` in turn, pulling out just the marks — read `map f lst` for now as "run `f` on each element of `lst`, and collect the results" (Chapter 7 covers `map` properly); here `f` is `snd`, so each `(name, mark)` pair contributes its `mark`.

A complete program pulls the same table apart four different ways:

```haskell
main :: IO ()
main = do
  print (length marks)
  print (fst (head marks))
  print (map snd marks)
  print (map fst marks)
```

Exact output:

```text
3
"Kago"
[72,68,91]
["Kago","Naledi","Thato"]
```

`map fst marks` mirrors `map snd marks` exactly, but pulls the other component out of every row — the names instead of the marks, in the same row order the table was written in.

<Quiz title="Check: Nesting" questions={[
  {
    question: "What is the type of marks = [(\"Kago\", 72), (\"Naledi\", 68), (\"Thato\", 91)]?",
    options: [
      "(String, Integer)",
      "[(String, Integer)]",
      "[String, Integer]",
      "([String], [Integer])"
    ],
    correctIndex: 1,
    explanation: "marks is a list whose elements are all pairs of type (String, Integer), so its type is a list of that pair type: [(String, Integer)].",
    optionFeedback: [
      "This is the type of one row, e.g. (\"Kago\", 72) — but marks itself is a whole list of these rows.",
      "Correct — marks is a list of (String, Integer) pairs.",
      "Square brackets around a comma-separated pair of types isn't valid Haskell type syntax for a list of pairs — the pair needs its own parentheses.",
      "This would be two separate lists (all the names, all the marks) kept apart — but marks keeps each name paired with its own mark."
    ]
  },
  {
    question: "Why is a list of tuples, rather than a tuple of lists, the natural fit for a table of students and marks?",
    options: [
      "Because tuples cannot contain strings",
      "Because each row (one student, one mark) is a fixed, small unit, while the number of rows can grow — matching tuple-for-row, list-for-the-varying-count exactly",
      "Because lists cannot contain tuples",
      "There's no real difference; both represent the same information equally well"
    ],
    correctIndex: 1,
    explanation: "Each row is fixed in shape (always exactly a name and a mark) — a job for a tuple. The number of rows is what varies as students are added — a job for a list. [(String, Integer)] matches both requirements at once: a list of fixed-shape rows.",
    optionFeedback: [
      "Tuples can hold a String perfectly well, as (\"Kago\", 72) demonstrates.",
      "Correct — a tuple's fixed shape suits one row, and a list's variable length suits the growing collection of rows.",
      "Lists can absolutely contain tuples — [(String, Integer)] is exactly that.",
      "The shapes genuinely differ: a tuple of lists (([String], [Integer])) separates names from marks and can desynchronize them; a list of tuples keeps each name locked to its own mark."
    ]
  }
]} />

## 7. Returning several results

A Haskell function returns one value. A tuple lets that one value carry several results:

```haskell
-- Return the sum and the absolute difference of two integers.
addAndDifference :: Integer -> Integer -> (Integer, Integer)
addAndDifference x y = (x + y, abs (x - y))
```

```text
ghci> addAndDifference 9 4
(13,5)
```

A complete program tries three pairs, including one where the arguments are swapped and one where they're equal:

```haskell
main :: IO ()
main = do
  print (addAndDifference 9 4)
  print (addAndDifference 4 9)
  print (addAndDifference 5 5)
```

Exact output:

```text
(13,5)
(13,5)
(10,0)
```

The first two lines are identical: `abs (x - y)` makes the difference symmetric, so swapping which argument is `x` and which is `y` changes nothing about either component. The third shows the zero case — two equal values sum to double one of them and differ by nothing.

The caller takes it apart with a pattern:

```haskell
report :: Integer -> Integer -> String
report x y = "Sum " ++ show total ++ ", difference " ++ show diff
  where
    (total, diff) = addAndDifference x y
```

<Quiz title="Check: Returning Several Results" questions={[
  {
    question: "Why does addAndDifference return one tuple instead of two separate Integer results?",
    options: [
      "Because Haskell functions cannot return more than one value directly — a tuple packages several results into the single value a function is allowed to return",
      "Because tuples run faster than returning two values",
      "Because Integer cannot be a return type on its own",
      "It's purely a style preference with no technical reason behind it"
    ],
    correctIndex: 0,
    explanation: "Every Haskell function returns exactly one value. When a computation logically produces more than one piece of information — here, a sum and a difference — the standard way to return them together is to bundle them into one tuple, which is itself a single value.",
    optionFeedback: [
      "Correct — a function returns exactly one value, so 'returning multiple results' really means returning one tuple that bundles them.",
      "This isn't about performance; it's about what a Haskell function's return type is even allowed to be.",
      "Integer works fine as a return type on its own, e.g. sumNumbers :: Integer -> Integer — that's not the constraint here.",
      "It reflects a real constraint: a function has exactly one return type, declared once in its signature."
    ]
  },
  {
    question: "In report, the where clause writes (total, diff) = addAndDifference x y. What is happening here?",
    options: [
      "A tuple pattern destructures the pair returned by addAndDifference, binding total and diff to its two components",
      "It calls addAndDifference twice, once for each name",
      "It creates a new tuple type called (total, diff)",
      "It is a type signature, not a value binding"
    ],
    correctIndex: 0,
    explanation: "(total, diff) is a tuple pattern, just like (x, y) in swap'. addAndDifference x y evaluates to one pair; the pattern takes it apart in one step, naming the first component total and the second diff, ready to use separately in the rest of report.",
    optionFeedback: [
      "Correct — this is ordinary tuple-pattern destructuring, matching the previous lesson's fst/snd-replacement patterns.",
      "addAndDifference x y is called exactly once here; the pattern just splits its one result into two named parts.",
      "(total, diff) is a pattern used in a binding, not a type declaration — no new type is introduced by this line.",
      "This line has no :: in it, so it's a value binding (like a where equation), not a type signature."
    ]
  }
]} />

## 8. Tuples as arguments

A tuple may also be the input, which is the curried-versus-tupled distinction from Chapter 3:

```haskell
add :: (Integer, Integer) -> Integer
add (x, y) = x + y
```

<ComparisonTable
  columns={[
    { header: 'Form', key: 'form' },
    { header: 'Signature', key: 'sig' },
    { header: 'Call', key: 'call' },
    { header: 'Use when', key: 'use' }
  ]}
  rows={[
    { form: 'Curried', sig: 'Integer -> Integer -> Integer', call: 'add 5 3', use: 'Normal Haskell style; supports partial application' },
    { form: 'Tupled', sig: '(Integer, Integer) -> Integer', call: 'add (5, 3)', use: 'The arguments genuinely arrive as one grouped value' }
  ]}
  highlightCol="use"
/>

Prefer the curried form unless the pair is a single conceptual thing — a coordinate, a key-value entry, a range.

These two shapes have standard names and standard converters in the Prelude: `curry :: ((a, b) -> c) -> a -> b -> c` turns a tupled function curried, and `uncurry :: (a -> b -> c) -> (a, b) -> c` does the reverse. Chapter 7's [Currying and Sections](/docs/sem3/csi243/07-higher-order/notes/currying-and-sections) lesson covers both in full, with worked examples — this chapter only needs the concept of the two shapes, not the conversion functions themselves.

<CommonMistake title="Passing a tuple to a curried function">
`add (5, 3)` looks like it should work, but if `add :: Integer -> Integer -> Integer` is curried, it doesn't — `(5, 3)` is a *single* pair, and `add` expects an `Integer` first, not a tuple. GHC reports a type error naming `(Integer, Integer)` where `Integer` was wanted. Either call `add 5 3` directly, or use `uncurry add (5, 3)` if the pair genuinely needs to travel together.
</CommonMistake>

<Quiz title="Check: Tuples as Arguments" questions={[
  {
    question: "Given add :: (Integer, Integer) -> Integer, which call is correct?",
    options: [
      "add 5 3",
      "add (5, 3)",
      "add 5, 3",
      "add [5, 3]"
    ],
    correctIndex: 1,
    explanation: "add's signature takes one argument, a pair. add (5, 3) supplies exactly that — one tuple value. add 5 3 treats add as curried, which this signature is not.",
    optionFeedback: [
      "This is how you'd call the curried version, Integer -> Integer -> Integer — but this add takes a single tupled argument instead.",
      "Correct — add expects one (Integer, Integer) argument, and (5, 3) is exactly that single tuple.",
      "This isn't valid Haskell syntax for a function call at all — arguments aren't comma-separated like this.",
      "add takes a tuple, not a list — [5, 3] is the wrong bracket and the wrong type entirely."
    ]
  },
  {
    question: "Why does the curried form support partial application while the tupled form doesn't?",
    options: [
      "The tupled form is simply an older, deprecated style",
      "A curried function can stop after supplying just the first argument, returning a function still waiting for the rest; a tupled function needs the whole tuple built and supplied in one go",
      "Tuples cannot be partially constructed at all, even outside function calls",
      "There's no real difference — both support partial application equally well"
    ],
    correctIndex: 1,
    explanation: "add 5 :: Integer -> Integer is a perfectly good, reusable value under the curried signature. Under the tupled signature, there is no way to supply 'just the first half' of one argument — the tuple itself is a single, complete value that must be constructed before it can be passed at all.",
    optionFeedback: [
      "Both forms are ordinary, current Haskell — the tupled form isn't deprecated, it's simply less convenient for partial application.",
      "Correct — currying's staged, one-argument-at-a-time structure is exactly what makes partial application possible.",
      "You can build (5, 3) as a value, sure — but a function expecting the whole tuple still needs it complete before it can produce a result; there's no partial version of that one argument.",
      "The whole point of this section's comparison table is that they behave quite differently here — that's why the curried form is recommended by default."
    ]
  }
]} />

## 9. List or tuple?

<ComparisonTable
  columns={[
    { header: 'Question', key: 'q' },
    { header: 'List', key: 'list' },
    { header: 'Tuple', key: 'tuple' }
  ]}
  rows={[
    { q: 'Length', list: 'May vary at run time', tuple: 'Fixed by its type' },
    { q: 'Element types', list: 'All the same', tuple: 'Each position may differ' },
    { q: 'Delimiters', list: 'Square brackets', tuple: 'Round brackets' },
    { q: 'Can you cons?', list: 'Yes, with :', tuple: 'No' },
    { q: 'Typical use', list: 'A collection of similar items', tuple: 'One record-like group, or several returned values' }
  ]}
  highlightCol="tuple"
/>

The deciding question: **can the number of components change?** If yes, you need a list. If no, and the parts mean different things, you need a tuple.

<RecognitionStrategy title="List or tuple, at a glance">
Ask one question: can the number of components change while the program runs? If yes — a growing set of scores, names, or words — use a list. If no — a fixed grouping like a coordinate, a date, or one row of a table — use a tuple, even if it only ever holds two values.
</RecognitionStrategy>

<Quiz title="Check: List or Tuple?" questions={[
  {
    question: "A function needs to represent one point in 3-D space: an x, a y, and a z coordinate. List or tuple?",
    options: [
      "A list, [x, y, z], since it's simplest to write",
      "A tuple, (x, y, z), since there are always exactly three coordinates",
      "Either is equally correct; the choice makes no difference",
      "A list, because coordinates are numbers and lists hold numbers"
    ],
    correctIndex: 1,
    explanation: "A 3-D point always has exactly three components, and that count never changes — a point can't suddenly gain a fourth coordinate. That fixed arity is exactly what a tuple's type guarantees; a list's length is, by design, allowed to vary, which would be the wrong signal to send here.",
    optionFeedback: [
      "A list would typecheck, but its type says nothing about there always being exactly three elements — a bug could silently produce a 2-element or 4-element 'point'.",
      "Correct — the count is fixed at exactly three, which is precisely what a tuple's type enforces and a list's type doesn't.",
      "The choice does matter: a tuple's fixed arity documents and enforces 'always exactly three', which a list's type cannot.",
      "Lists can hold numbers, but so can tuples — the deciding factor here is whether the count can vary, not the element type."
    ]
  },
  {
    question: "A function reads in a sentence and needs to hold onto its words. List or tuple?",
    options: [
      "A tuple, since all the words are String and belong together",
      "A list, since the number of words in a sentence varies from sentence to sentence",
      "A tuple, since a sentence is one grammatical unit",
      "Neither works for holding words"
    ],
    correctIndex: 1,
    explanation: "The number of words is unpredictable and changes from input to input — exactly the situation a list is for. That every word is a String (all the same type) reinforces this: tuples group different-typed things in a fixed count, lists group same-typed things in a variable count.",
    optionFeedback: [
      "Sharing a type is actually a signal toward a list, not a tuple — tuples are for grouping different kinds of things in fixed positions.",
      "Correct — a sentence's word count varies, which is exactly what a list's variable length is built to represent.",
      "The word count differing between sentences is the deciding factor, and it points squarely at a list, not a tuple's fixed shape.",
      "[String] handles this well — a list of words is a completely standard way to represent this in Haskell."
    ]
  }
]} />

## 10. Common errors

| Attempt | Problem | Repair |
|---|---|---|
| `fst (1,2,3)` | `fst` works only on pairs | Use a pattern `(a,_,_)` |
| `(5)` treated as a tuple | Arity 1 does not exist | `(5, x)` or just `5` |
| `(1,2) == (2,1)` expected `True` | Position is part of the value | Compare the intended components |
| `x : (1,2)` | Tuples have no cons | Use a list if it must grow |
| Passing `(5,3)` to `add :: Int -> Int -> Int` | That signature is curried | `add 5 3`, or change the signature |
| `[(1,'a'), (2,3)]` | List elements must share one type | Make every tuple the same type |

## 11. Practice

1. Use `fst` and `snd` together to extract the character from `((1,'a'), "foo")`.
2. Write `swap :: (a,b) -> (b,a)` and give its type by inspection before checking with `:type`.
3. Write `addAndDifference` and test it on two equal values.
4. Build a `[(String, Integer)]` of three students and marks, then write `bestMark` returning the highest mark.
5. State whether each needs a list or a tuple, and why: a date; the marks of a class; a point in 3-D space; the words in a sentence.

<Accordions>
<Accordion title="Check questions 1 and 5">

**1.** `snd (fst ((1,'a'), "foo"))`. The inner `fst` gives `(1,'a')`, then `snd` gives `'a'`.

**5.** A date is a tuple — always three parts of different meaning. Class marks are a list — the count varies. A 3-D point is a tuple — always exactly three coordinates. Words in a sentence are a list — the count varies, and every word is the same kind of thing.

</Accordion>
</Accordions>

<Checklist title="Mastery check" rules={[
  { type: 'must', text: 'I can define arity and say why arity 1 does not exist.' },
  { type: 'must', text: 'I can explain why (String, Integer) and (Integer, String) are different types.' },
  { type: 'must', text: 'I can use fst, snd, and tuple patterns, including nested ones.' },
  { type: 'must', text: 'I can return several results from one function using a tuple.' },
  { type: 'must', text: 'I can represent a table as a list of tuples.' },
  { type: 'must', text: 'I can decide between a list and a tuple by asking whether the count can change.' },
  { type: 'avoid', text: 'Do not use fst or snd on anything but a pair.' }
]} />

Continue with [List Comprehensions](/docs/sem3/csi243/06-lists-tuples/notes/list-comprehensions).
