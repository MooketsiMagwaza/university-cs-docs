---
title: "1. Programming and the Functional Model"
description: "Build a zero-assumption mental model of programming, input, processing, output, storage, values, expressions, pure functions, immutability, and controlled effects."
---



This is the real starting point. You do **not** need to know Haskell, programming, functions, or computer science vocabulary before reading it.

By the end, you should be able to explain what a program does, trace information through input–process–output–storage, and describe why Haskell separates pure calculations from interactions with the outside world.

A program receives information, transforms it according to precise rules, and makes the result useful:

> **information enters → rules transform it → a result is produced → the result may be displayed or saved**

## 1. What is programming?

A **computer** is a machine that can follow precise instructions. It is fast, but it does not guess what you meant. A **program** is a set of instructions or definitions written precisely enough for the computer to follow.

**Programming** is the work of:

1. understanding a problem;
2. deciding what information is needed;
3. designing rules that transform that information;
4. expressing those rules in a programming language;
5. testing whether the result is correct.

A **programming language** is a language with strict rules for communicating programs to a computer. Haskell is the programming language used in CSI243.

### A problem before any Haskell

Suppose a shop needs to calculate the total price of three notebooks at R20 each.

- The known information is **3 notebooks** and **R20 per notebook**.
- The rule is **quantity multiplied by price**.
- The result is **R60**.

Haskell can express that calculation as an **expression**:

```haskell
-- Multiply the quantity, 3, by the price per notebook, 20.
3 * 20
```

The exact result is:

```text
60
```

The symbol `*` means multiplication. The expression does not ask for input and does not print anything by itself. It simply describes a calculation whose value is `60`.

## 2. Input, processing, output, and storage

These four words help us separate the responsibilities inside a program.

| Part | Plain meaning | Temperature-program example |
|---|---|---|
| **Input** | Information entering the program | A user supplies `25` degrees Celsius |
| **Processing** | Rules that transform information | Convert Celsius to Fahrenheit |
| **Output** | Information leaving the program in a useful form | Show `77` on the screen |
| **Storage** | Keeping information so it can be used later | Save the conversion in a file or database |

Not every program needs every stage. A calculator can produce an answer without saving it. A program can process information that was already placed in its source file. Storage is optional unless the information must survive for later use.

<Mermaid chart={`flowchart LR
  A[Input] --> B[Processing]
  B --> C[Output]
  B --> D[Storage]`} caption="Storage branches off processing rather than following output, because a program can display a result, save it, both, or neither — the two are independent choices, not two names for the same step." />

<StepByStep>
  <StepByStepTitle>Trace a temperature conversion</StepByStepTitle>

  <Step number={1}>
  **Input:** the number `25` enters the program. At this point it is data, not yet the answer.
  </Step>

  <Step number={2}>
  **Processing:** the program applies the conversion rule: multiply by `9`, divide by `5`, then add `32`.
  </Step>

  <Step number={3}>
  **Result:** the calculation produces the value `77`.
  </Step>

  <Step number={4}>
  **Output:** the program can display `77°F`. If it also saves the result for tomorrow, that separate action is storage.
  </Step>
</StepByStep>

Here is only the processing rule in Haskell:

```haskell
-- Convert the Celsius value 25 to Fahrenheit.
-- The parentheses force Haskell to calculate 25 * 9 before dividing by 5.
(25 * 9) / 5 + 32
```

The exact result is:

```text
77.0
```

The `.0` shows that the result is represented as a number that can contain a fractional part.

### “Storage” can mean different things

Beginners often hear the word *store* used too loosely. We will keep four meanings separate:

| Meaning | What it actually means | Example |
|---|---|---|
| **Binding a name** | Give a value a stable name inside the program | Let `price` mean `20` |
| **Data structure** | Group values so the program can work with them | A list of test scores |
| **Source file** | Save program text in a `.hs` file | Save a function in `Main.hs` |
| **Persistent storage** | Save data outside the running program | Write results to a file or database |

Binding a name is **not** the same as saving information permanently. A name used during a calculation disappears when that program session ends unless the information is written somewhere external.

<Quiz title="Check: input, processing, output, storage" questions={[
  {
    question: "A calculator program reads two numbers, adds them, and shows the sum. It saves nothing. Which stage from the model is genuinely absent?",
    options: ["Input", "Processing", "Output", "Storage"],
    correctIndex: 3,
    explanation: "The section is explicit: storage is optional. A program can produce a correct, complete output without keeping the result anywhere once it has been shown.",
    optionFeedback: [
      "Input is present: the two numbers have to enter the program somehow before anything can be added.",
      "Processing is present: adding the two numbers together is exactly the transformation step.",
      "Output is present: showing the sum on the screen is the program's output stage.",
      "Correct. Displaying a result and saving a result are two different, independent jobs; this program only does the first."
    ]
  },
  {
    question: "According to the four meanings of 'store' introduced in this lesson, what is `let price = 20` an example of?",
    options: ["Persistent storage", "A data structure", "Binding a name", "A source file"],
    correctIndex: 2,
    explanation: "Giving a value a stable name inside a running program is binding, the first and weakest of the four meanings. It disappears when the session ends unless written somewhere external.",
    optionFeedback: [
      "Persistent storage means saving data outside the running program, such as to a file or database — nothing external has happened here.",
      "A data structure groups multiple values together, such as a list; a single named value is not a data structure by itself.",
      "Correct. `price` is a stable label for `20` for as long as the current scope exists — that is binding, not permanent storage.",
      "Nothing has been saved to a `.hs` file here; this is a name introduced during a calculation, not saved program text."
    ]
  }
]} />

## 3. What is a programming paradigm?

A **paradigm** is a general style or model of thinking. A **programming paradigm** is a general way to organise a solution and describe computation.

Different paradigms can solve the same problem while encouraging different questions.

| Paradigm | Main question | Typical description |
|---|---|---|
| **Imperative** | “Which commands should change the program’s state, and in what order?” | Set a total, update it repeatedly, then display it |
| **Object-oriented** | “Which objects own the data and behaviour?” | Ask a cart object to calculate its total |
| **Logic** | “Which facts and rules must be true?” | State price facts and ask which total follows |
| **Functional** | “Which values and transformations produce the result?” | Transform a list of prices into one total |

A language can support more than one paradigm. The label describes a strong design style, not a prison.

<Quiz title="Check: programming paradigms" questions={[
  {
    question: "A cart total can be computed by setting a running total and updating it, by asking a cart object to calculate its own total, or by transforming a list of prices into one total. According to the table above, which paradigm does the last description belong to, and why?",
    options: [
      "Imperative, because it still involves numbers",
      "Object-oriented, because a cart is mentioned",
      "Functional, because its main question is 'which values and transformations produce the result', matching 'transform a list of prices into one total'",
      "Logic, because it states a rule about prices"
    ],
    correctIndex: 2,
    explanation: "The table's functional row gives exactly this typical description: 'Transform a list of prices into one total.' The imperative row instead updates a running total step by step, and the object-oriented row asks an object to calculate its own total — different questions entirely, even though all three could solve the identical problem.",
    optionFeedback: [
      "Numbers appear in every paradigm's version of this problem; what distinguishes them is the main question each one asks, not whether numbers are involved.",
      "Mentioning a cart is not what makes something object-oriented; the object-oriented row's defining trait is asking which object owns the data and behaviour, e.g. 'ask a cart object to calculate its total'.",
      "Correct. This is the exact typical description listed for the functional paradigm's row: transforming values, not issuing commands or asking an object to act.",
      "Logic programming states facts and rules and asks which conclusions follow; nothing here declares standing price facts to be queried."
    ]
  }
]} />

## 4. What is functional programming?

**Functional programming** is a paradigm that models a program primarily as the evaluation of expressions and the application of functions to values.

**In plain English:** a functional program is written by describing what values look like and how to turn one value into another — never by listing a sequence of commands that change things in place. This kind of dense, formal sentence shows up constantly in lecture slides and lab sheets; [Decoding Exam and Lab Language](/docs/sem3/csi243/decoding-questions) teaches a repeatable method for turning that phrasing into plain English before you try to write any Haskell.

It emphasises:

- **values** rather than changing boxes;
- **expressions** that produce values;
- **pure functions** that transform inputs into outputs predictably;
- **immutability**, so an existing value is not silently changed;
- **composition**, meaning small transformations can be connected;
- **controlled effects**, so interaction with the outside world remains visible.

Functional programming does not merely mean “uses functions.” Python, Java, and many other languages have functions. The functional model is distinguished by its emphasis on values, expressions, purity, immutability, composition, and explicit effects.

### Why use functional programming?

Functional programming is useful because predictable pieces are easier to:

- test with known inputs and expected outputs;
- understand without tracking many hidden changes;
- reuse in other parts of a program;
- combine into larger transformations;
- run independently or concurrently when they do not share changing state;
- refactor with confidence when their types and behaviour stay the same.

It does not make every problem automatically easy or every program automatically fast. It gives us a disciplined way to separate stable reasoning from changing outside-world interactions.

### When is it useful?

The functional model works especially well for:

- calculations;
- validating information;
- transforming collections of data;
- compilers and parsers;
- financial rules;
- server-side data processing;
- concurrent systems;
- any problem that can be expressed as a pipeline of transformations.

Real applications still need input, output, files, time, randomness, and networks. Haskell does not remove those operations. It marks them so that we can tell them apart from pure calculations.

<Quiz title="Check: what functional programming is" questions={[
  {
    question: "A classmate says, \"Java has functions too, so Java must already be functional programming.\" What is the strongest reply, based on this lesson?",
    options: [
      "They are right — any language with functions counts as functional.",
      "They are wrong — functional programming is distinguished by its emphasis on values, purity, immutability, composition, and explicit effects, not merely the presence of functions.",
      "They are wrong — functional programming means a program contains no loops.",
      "They are wrong — functional programming only applies to mathematics, not real programs."
    ],
    correctIndex: 1,
    explanation: "The lesson says this directly: 'Functional programming does not merely mean \"uses functions.\"' Many paradigms have functions; the functional model is distinguished by what it emphasises about values, purity, immutability, composition, and effects.",
    optionFeedback: [
      "This repeats the classmate's mistake. Having functions is necessary but nowhere near sufficient — object-oriented and imperative languages have functions too.",
      "Correct — this is the precise distinction the lesson draws, and it is the one to reach for whenever 'it has functions' is offered as the whole definition.",
      "Loops are an imperative-programming detail, not the defining line. The real distinction is about values, purity, and effects, not about which control-flow keywords exist.",
      "Functional programming is used in real, deployed software — compilers, servers, financial systems. The paradigm is a way of organising a solution, not a restriction to theoretical mathematics."
    ]
  },
  {
    question: "Why does the lesson say functional programming is useful, rather than claiming it makes every program automatically fast or easy?",
    options: [
      "Because predictable, pure pieces are easier to test, understand, reuse, and combine — not because purity guarantees performance or effortless solutions.",
      "Because functional programs always run faster than imperative ones.",
      "Because functional programming eliminates the need to think about algorithms.",
      "Because functional programming removes the need for input and output entirely."
    ],
    correctIndex: 0,
    explanation: "The lesson is explicit that functional programming 'does not make every problem automatically easy or every program automatically fast.' Its value is in disciplined separation: predictable pieces are easier to test, reuse, and reason about.",
    optionFeedback: [
      "Correct — the stated benefits are about testability, understandability, reuse, and composition, with an explicit disclaimer against 'automatically fast' claims.",
      "The lesson makes no such performance claim, and purity alone says nothing about an algorithm's efficiency.",
      "You still need to design correct algorithms; functional programming changes how you express and reason about them, not whether you need them.",
      "Real applications still need input, output, files, time, randomness, and networks — the lesson says so directly. Functional programming marks these, it does not remove them."
    ]
  }
]} />

## 5. Values: the results we work with

A **value** is a piece of data. These are four examples:

```haskell
-- A whole-number value.
4

-- A Boolean value, meaning true.
True

-- A single character value. A character uses single quotation marks.
'H'

-- A text value. Text uses double quotation marks.
"Hello"
```

If entered one at a time in GHCi, the exact displayed results are:

```text
4
True
'H'
"Hello"
```

The quotation marks are part of how Haskell represents characters and text in code. They tell Haskell that `"Hello"` is text rather than the name of something in the program.

| Written form | Meaning | Kind of value |
|---|---|---|
| `4` | The whole number four | Number |
| `True` | A logical true value | Boolean |
| `'H'` | One character | Character |
| `"Hello"` | Five characters of text | String |

## 6. Expressions: code that produces values

An **expression** is Haskell code that can be evaluated to produce a value.

To **evaluate** means to follow the language’s rules until the resulting value is known.

```haskell
-- Add 2 and 3. The expression evaluates to 5.
2 + 3

-- Compare 10 with 4. The expression evaluates to True.
10 > 4

-- Choose one text value based on the Boolean condition 70 >= 50.
if 70 >= 50 then "Pass" else "Fail"
```

Entered one at a time, the exact results are:

```text
5
True
"Pass"
```

The last example is read as: “if 70 is greater than or equal to 50, produce the text `Pass`; otherwise produce the text `Fail`.” Both possible paths produce a value.

### Expression versus action

| Expression that calculates a value | Action that interacts with the outside world |
|---|---|
| `2 + 3` produces `5` | Printing `5` makes information appear on a terminal |
| `10 > 4` produces `True` | Reading a key obtains information from a user |
| A conversion produces `77.0` | Saving `77.0` changes a file or database |

This distinction becomes essential in Haskell: **calculating a result and displaying a result are two different jobs**.

## 7. Functions: reusable transformations

A **function** receives an input and produces an output. The rule can be reused with different inputs.

Consider a function that cubes a number. To **cube** a number means to multiply it by itself three times.

```haskell
-- The type signature is the function's contract.
-- Read it as: "cube receives an Integer and produces an Integer."
cube :: Integer -> Integer

-- x is the name given to the input for this calculation.
-- The expression on the right produces the output.
cube x = x * x * x
```

New notation introduced here:

| Part | Read it as | Meaning |
|---|---|---|
| `cube` | “cube” | The function’s name |
| `::` | “has type” | Separates a name from its type |
| `Integer` | “integer” | A whole-number type |
| `->` | “to” or “produces” | Separates input type from output type |
| `x` | “x” | A local name for the supplied input |
| `=` | “is defined as” | Separates the function pattern from its result expression |

Testing the function in GHCi:

```haskell
-- Apply cube to the input 4.
cube 4
```

The exact result is:

```text
64
```

<StepByStep>
  <StepByStepTitle>How Haskell evaluates cube 4</StepByStepTitle>

  <Step number={1}>
  Find the definition `cube x = x * x * x`.
  </Step>

  <Step number={2}>
  The supplied input is `4`, so the local name `x` means `4` during this evaluation.
  </Step>

  <Step number={3}>
  Replace `x` with `4`, producing `4 * 4 * 4`.
  </Step>

  <Step number={4}>
  Evaluate the multiplication, producing the final value `64`.
  </Step>
</StepByStep>

The space in `cube 4` means **apply the function `cube` to the value `4`**. Haskell commonly writes function application with a space rather than parentheses and commas.

<Quiz title="Check: functions and signatures" questions={[
  {
    question: "cube :: Integer -> Integer is read as \"cube receives an Integer and produces an Integer.\" In cube x = x * x * x, what job does the -> in the signature actually correspond to?",
    options: [
      "It corresponds to the multiplication symbol * in the definition",
      "It separates the input type from the output type — it says nothing about how the result is computed, only what kind of value goes in and what kind comes out",
      "It means cube takes zero arguments",
      "It is only decorative and can be removed without changing the meaning"
    ],
    correctIndex: 1,
    explanation: "The lesson's own table states this directly: '->' is read as 'to' or 'produces', and it 'separates input type from output type.' The actual computation, x * x * x, is a completely separate part of the definition from the type signature.",
    optionFeedback: [
      "The signature describes types, not operations; * appears only in the equation cube x = x * x * x, never in the type Integer -> Integer itself.",
      "Correct. This is exactly the notation table's own reading: -> separates the input type from the output type, independent of how the function computes its result.",
      "cube 4 in the worked trace clearly supplies one argument, 4, which becomes x; the signature's single -> matches one input and one output, not zero inputs.",
      "The signature is meaningful, not decorative: GHC checks the implementation against it, and removing -> would leave Integer Integer, which is not valid Haskell at all."
    ]
  }
]} />

## 8. Pure functions

A function is **pure** when both statements are true:

1. Its result depends only on the inputs supplied to it.
2. Evaluating it does not cause an outside-world effect.

The `cube` function is pure. `cube 4` always produces `64`. It does not read the keyboard, print a message, change a file, or ask the clock for the current time.

Another pure function:

```haskell
-- Calculate a final price after subtracting a discount.
-- Both the price and discount are explicit inputs.
finalPrice :: Double -> Double -> Double
finalPrice price discount = price - discount
```

New notation: `Double` is a numeric type that can represent fractional values. The signature contains two input types followed by the output type.

```haskell
-- Supply the price 100.0 and the discount 15.0.
finalPrice 100.0 15.0
```

The exact result is:

```text
85.0
```

### Why purity helps

If a function’s inputs are visible, you have everything needed to predict and test its result. There is no hidden changing variable, file, clock, or user action that silently changes the answer.

| Pure-function property | Practical benefit |
|---|---|
| Same inputs, same output | Tests are repeatable |
| No hidden outside interaction | Behaviour is easier to explain |
| Does not mutate shared data | Independent work is safer |
| Produces a reusable value | It can be combined with other functions |

### One more pure function, to make the pattern concrete

The same two rules — result depends only on the inputs, evaluating it touches nothing outside — apply just as well to a function that answers a yes/no question:

```haskell
-- Decide whether an already-supplied score counts as a pass.
-- Both statements of purity hold: the answer depends only on score,
-- and checking it prints nothing, saves nothing, and reads nothing.
isPassingScore :: Integer -> Bool
isPassingScore score = score >= 50
```

```text
ghci> isPassingScore 42
False
ghci> isPassingScore 73
True
ghci> isPassingScore 73
True
```

Calling `isPassingScore 73` a second time gives exactly the same answer as the first, with nothing in between that could have changed it. That repeatability — not the fact that a function is "short" — is what purity buys you.

<Quiz title="Check: pure functions" questions={[
  {
    question: "Which of these, described in plain English, is a pure calculation rather than an effect?",
    options: [
      "Asking the user to type their name",
      "Deciding whether an already-known score of 73 is at least 50",
      "Printing 'Pass' to the terminal",
      "Reading the system clock to log the current time"
    ],
    correctIndex: 1,
    explanation: "The comparison 73 >= 50 depends only on the two numbers already in hand. It does not read from, or write to, anything outside the calculation.",
    optionFeedback: [
      "Asking the user to type something is an interaction with the outside world — an effect, not a pure calculation.",
      "Correct. Both purity conditions hold: the answer depends only on 73 and 50, and deciding it touches nothing external.",
      "Printing to the terminal changes what is visible outside the program — that is an effect, even though the value being printed might itself have been computed purely.",
      "The clock can report a different value each time it is read, and reading it is an interaction with the machine's environment — an effect."
    ]
  },
  {
    question: "finalPrice 100.0 15.0 is called twice in the same program. Based on purity, what should you expect?",
    options: [
      "The second call might return a different value if the store's prices changed in the meantime.",
      "The second call returns exactly 85.0 again, because the result depends only on the two arguments supplied, not on anything external.",
      "The second call is faster because the first call already 'warmed up' the function.",
      "The second call will fail, because a pure function cannot be called more than once."
    ],
    correctIndex: 1,
    explanation: "finalPrice's result depends only on price and discount, the two values actually passed in. Nothing about calling it earlier changes what it does the next time it is called with the same arguments.",
    optionFeedback: [
      "This is exactly the kind of hidden dependency purity rules out. finalPrice never looks at a store's current prices — it only ever looks at the two numbers you hand it.",
      "Correct. Same inputs in, same output out, every time — that predictability is the entire point of the definition of purity used in this lesson.",
      "Purity is about correctness and predictability, not about caching or speed. A pure function may be recomputed from scratch on every call.",
      "There is nothing in the definition of purity that limits how many times a function can be called; the opposite is true — a pure function is safe to call as often as you like."
    ]
  }
]} />

## 9. Immutability and bindings

To **bind** a name means to give a value a name. In beginner Haskell, think of the name as a stable label for a value, not a storage box whose contents are repeatedly replaced.

```haskell
-- Bind the name taxRate to the value 0.15.
taxRate :: Double
taxRate = 0.15
```

The name `taxRate` means `0.15` in its scope. Haskell’s `=` is read here as **“is defined as”**, not “take an existing box and overwrite it.”

This is different from a changing-variable story:

| Immutable binding | Mutable variable |
|---|---|
| A name has a stable meaning in its scope | A storage location may contain different values over time |
| Reason from the definition and inputs | Track every operation that might update the location |
| Favoured by Haskell’s pure code | Common in imperative programming |

A Haskell program can still receive a new score from a user or read an updated file. Immutability means that an existing pure value is not secretly changed in place; new information becomes a new value.

### The same pattern in a different domain

`taxRate` is not special. Any name bound this way behaves identically:

```haskell
-- Bind the name interestRate to the value 0.05.
interestRate :: Double
interestRate = 0.05
```

`interestRate` means `0.05` for the rest of its scope, for exactly the same reason `taxRate` means `0.15` for the rest of its scope. There is nothing about tax specifically that makes bindings immutable — the rule is about `=`, not about the subject matter.

### Rebinding is not mutation — even in GHCi

GHCi's `let` can make this look confusing at first, because you *can* type `let score = ...` twice in one session:

```text
ghci> let score = 72
ghci> score
72
ghci> let score = 90
ghci> score
90
```

It looks as though `score` changed from `72` to `90`. It did not. The second `let` does not reach back and overwrite the first binding — it introduces a **new, separate** binding that happens to reuse the name `score`, and the new one is the one now in scope. Nothing that already used the value `72` gets rewritten; if some other expression had captured `score` as `72` before the second `let`, that expression would still mean `72`. This is exactly the "new information becomes a new value" rule from the paragraph above, applied at the GHCi prompt instead of inside a script.

<Quiz title="Check: immutability" questions={[
  {
    question: "After running `let score = 72` and then `let score = 90` in the same GHCi session, what actually happened to the first binding?",
    options: [
      "It was overwritten in place, so only one `score` ever existed.",
      "It still exists unchanged as `72`; the name `score` now simply refers to a newer, separate binding instead.",
      "GHCi raised an error, because a name cannot be bound twice.",
      "Haskell deleted the value `72` from memory the moment the second `let` ran."
    ],
    correctIndex: 1,
    explanation: "Each `let` introduces its own binding. The second `let score = 90` does not reach back and mutate the first one — it shadows it, so the name `score` now resolves to the newer binding while the concept of '72' itself was never rewritten.",
    optionFeedback: [
      "This is the mutable-variable story, which is exactly what immutability rules out. Nothing was overwritten in place; a new binding was introduced instead.",
      "Correct. This is rebinding, not mutation — the distinction the lesson is building toward.",
      "GHCi accepts this without complaint; shadowing a name at the prompt is normal and does not error.",
      "Haskell does not 'delete' values from memory when a name is shadowed; the old binding simply becomes unreachable by that name in the current scope."
    ]
  },
  {
    question: "Why does the lesson introduce `interestRate` right after `taxRate`, using the exact same shape?",
    options: [
      "To show that immutability is a special rule that only applies to money-related values.",
      "To show that the rule — a name means one stable value in its scope — is about the `=` binding itself, not about the particular subject (tax, interest, or anything else).",
      "Because `interestRate` and `taxRate` must always be bound to the same number.",
      "To introduce a new keyword needed for financial calculations."
    ],
    correctIndex: 1,
    explanation: "Using an unrelated domain (interest rather than tax) with the identical binding shape demonstrates that immutability is a general property of Haskell's `=`, not a special case that happens to apply to tax calculations.",
    optionFeedback: [
      "Immutability is a general property of every binding in Haskell, not a rule specific to financial values — the second example exists precisely to show that.",
      "Correct. The point of repeating the pattern in a new domain is to separate 'what the rule is' from 'which example first illustrated it.'",
      "The two names are completely independent; nothing requires them to share a value. They just happen to both be bound with `=`.",
      "No new syntax is introduced here — `interestRate = 0.05` uses exactly the same `::` and `=` already seen with `taxRate`."
    ]
  }
]} />

## 10. Effects: interaction with the outside world

An **effect** is an interaction beyond simply returning a value. Common effects include:

- reading keyboard input;
- printing to a terminal;
- reading or writing a file;
- obtaining the current time;
- generating randomness;
- communicating over a network.

Effects are necessary. A useful application must eventually interact with something. The difficulty is that the outside world can change: users type different input, files are edited, and networks fail.

Haskell therefore keeps this distinction visible:

| Pure centre | Effectful boundary |
|---|---|
| Transform already supplied values | Obtain values from outside the program |
| Predictable from inputs | May depend on a changing environment |
| Easy to test directly | Often needs a controlled test environment |
| Produces a value | Performs or describes an outside interaction |

### The boundary comes before its syntax

A complete application usually has an effectful boundary that obtains input and displays or saves output. Its pure centre transforms the supplied values. At this point, you only need that distinction; the exact Haskell output functions and program structure are introduced later, one component at a time.

<Quiz title="Check: effects and the pure/effectful boundary" questions={[
  {
    question: "A weather app reads a city name typed by the user, calculates whether it will feel cold (below 15°C), and displays 'Cold' or 'Mild'. Which single word best names what the calculation step is?",
    options: ["An effect", "The pure centre", "Storage", "A paradigm"],
    correctIndex: 1,
    explanation: "Deciding cold-or-mild from an already-known temperature depends only on that number. It is the pure centre: predictable from its input, and it performs no outside interaction by itself.",
    optionFeedback: [
      "The calculation itself reads nothing and writes nothing outside itself — reading the city name and displaying the result are the effects, not the comparison.",
      "Correct. The temperature comparison is exactly the kind of already-supplied-values transformation the lesson calls the pure centre.",
      "Nothing is being kept for later use here — no file or database is written, so storage does not apply to this step.",
      "A paradigm is a general style of organising computation (functional, imperative, and so on); it is not a name for one step of one program."
    ]
  },
  {
    question: "Why does the lesson insist that Haskell keeps effects 'visible' rather than simply saying effects are avoided?",
    options: [
      "Because Haskell programs are not allowed to read input or print output.",
      "Because a useful application must eventually interact with something outside itself, so the goal is marking that interaction clearly, not pretending it does not exist.",
      "Because effects always run before any pure calculation in a Haskell program.",
      "Because Haskell converts every effect into a pure calculation automatically."
    ],
    correctIndex: 1,
    explanation: "The lesson states this directly: 'effects are necessary,' and Haskell's answer is to keep the boundary between pure and effectful code visible rather than to eliminate effects, which would make the language useless for real applications.",
    optionFeedback: [
      "Haskell programs read input and produce output constantly — the point is that these actions are marked, not forbidden.",
      "Correct. Effects are unavoidable in a useful program; the functional discipline is about making the boundary explicit, not about pretending the outside world does not exist.",
      "Nothing in the model requires effects to run before pure calculations; a program can compute a great deal purely before ever touching the outside world.",
      "An effect and a pure calculation remain distinct; wrapping an effect in IO does not turn it into a pure calculation, it marks it as impure so the difference stays visible."
    ]
  }
]} />

## 11. Functional and imperative mental models

Imagine calculating the total of three prices.

### Imperative-style story

1. Create a changing variable called `total` with the value zero.
2. Add the first price into `total`.
3. Replace `total` with the new value.
4. Repeat for the remaining prices.
5. Read the final value from `total`.

### Functional-style story

1. Begin with the three price values.
2. Apply a reusable summing transformation.
3. Use the value produced by that transformation.

| Question | Imperative emphasis | Functional emphasis |
|---|---|---|
| What is central? | Commands and changing state | Values and transformations |
| How is progress described? | Update locations step by step | Evaluate expressions and connect functions |
| What must the reader track? | Current state after each command | Inputs, transformations, and resulting values |
| Where are effects? | May be mixed through the instructions | Kept visible and separated where practical |

Neither comparison means that computers stop using memory or executing instructions. It describes the **programmer’s model for expressing and reasoning about the solution**.

## 12. The functional program shape

Use this model whenever you meet a new program:

<StepByStep>
  <StepByStepTitle>Input → transform → result → effect</StepByStepTitle>

  <Step number={1}>
  **Find the input.** Ask where each starting value comes from: a literal in the program, a function argument, a user, or a file.
  </Step>

  <Step number={2}>
  **Find the pure transformation.** Ask which functions calculate new values without interacting with the outside world.
  </Step>

  <Step number={3}>
  **Find the result.** State the exact value and its meaning.
  </Step>

  <Step number={4}>
  **Find the effect.** Ask whether the result is printed, saved, transmitted, or otherwise used outside the pure calculation.
  </Step>
</StepByStep>

### Worked classification

A marks program receives `70`, decides that scores of at least `50` pass, displays `Pass`, and saves the result.

| Stage | Program responsibility | Pure or effectful? |
|---|---|---|
| Input | Receive `70` from the user | Effectful |
| Processing | Compare `70` with `50` | Pure |
| Result | Produce the text value `"Pass"` | Pure value |
| Output | Display `Pass` | Effectful |
| Storage | Save the result to a file | Effectful |

This separation is one of the most important habits in CSI243.

### A second worked classification, without storage

Not every program needs a Storage row. A currency-converter program reads two numbers typed by the user, multiplies by a fixed exchange rate, and shows the converted amount — and stops there.

| Stage | Program responsibility | Pure or effectful? |
|---|---|---|
| Input | Receive the amount and the exchange rate from the user | Effectful |
| Processing | Multiply the amount by the rate | Pure |
| Result | Produce the converted number | Pure value |
| Output | Display the converted amount | Effectful |

There is no Storage row because nothing here needs to survive the program ending — exactly the "storage is optional" point from Section 2, now seen applied to a full example rather than stated as a rule.

<Quiz title="Check: worked classification" questions={[
  {
    question: "In the currency-converter example, why does 'multiply the amount by the rate' count as pure even though the amount originally came from user input?",
    options: [
      "It does not count as pure; anything downstream of user input is automatically effectful too.",
      "Because by the time the multiplication runs, the amount and rate are already-known values; the multiplication itself reads nothing further from, and writes nothing to, the outside world.",
      "Because multiplication is a mathematical operation, and all mathematical operations are automatically pure regardless of context.",
      "Because the result is eventually displayed on the screen."
    ],
    correctIndex: 1,
    explanation: "Purity is a property of the calculation step itself, not of where its inputs originally came from. Once the amount and rate are in hand as ordinary values, multiplying them touches nothing outside the calculation.",
    optionFeedback: [
      "This conflates the input step with the processing step. The two are separate rows in the table for exactly this reason: obtaining a value and transforming an already-obtained value are different jobs.",
      "Correct. The lesson's definition of purity only asks about the calculation step itself: does its result depend only on its inputs, and does evaluating it avoid outside interaction? Both hold here.",
      "Multiplication itself is pure, but that is a coincidence of this example, not a rule — a function could be built around multiplication and still perform an effect elsewhere in its body.",
      "Being displayed later is the Output row's job, a separate effectful step. It does not retroactively make the Processing row effectful."
    ]
  }
]} />

## 13. Common misconceptions

| Misconception | Correction |
|---|---|
| “Functional programming just means using functions.” | Many paradigms use functions. Functional programming specifically emphasises values, expressions, purity, immutability, composition, and controlled effects. |
| “Pure means clean-looking code.” | Purity has a precise meaning: the output depends only on inputs, and evaluation causes no outside-world effect. |
| “Effects are bad.” | Effects are necessary for interaction. Haskell makes them explicit so that they do not become hidden dependencies. |
| “Immutable means the program cannot handle change.” | The world can supply new values. Immutability prevents an existing pure binding from being silently overwritten. |
| “An expression automatically prints its value.” | An expression calculates a value. GHCi may show it for convenience, but a saved program needs an output action to display it. |
| “Binding a name saves data permanently.” | A binding gives a value a name inside a scope. Persistent storage requires an external file, database, or similar system. |

## 14. Guided practice

Classify each responsibility as **input**, **processing**, **output**, or **storage**:

1. A user types a radius.
2. The program calculates the circle’s area.
3. The area appears on the terminal.
4. The program writes the area to a report file.

Then classify each item as a **pure calculation** or an **effect**:

1. Multiply an already supplied radius by itself.
2. Read the radius from the keyboard.
3. Decide whether an already supplied score is at least 50.
4. Print `Pass` on the terminal.

<Accordions>
<Accordion title="Check your classifications">

- Radius from a user: input and an effect.
- Area calculation: processing and pure.
- Terminal display: output and an effect.
- Report file: storage and an effect.
- Multiplication: pure.
- Keyboard read: effectful.
- Pass comparison: pure.
- Terminal print: effectful.

</Accordion>
</Accordions>

<Checklist title="Mastery check" rules={[
  { type: 'must', text: 'I can trace a program through input, processing, output, and optional storage, and say why storage is the one stage that can be skipped.' },
  { type: 'must', text: 'I can name the question a paradigm asks (imperative, object-oriented, logic, functional) and give a typical description for each.' },
  { type: 'must', text: 'I can explain functional programming by its emphasis on values, expressions, purity, immutability, composition, and controlled effects — not by saying only "it uses functions".' },
  { type: 'must', text: 'I can classify a value, an expression, a pure function, and an effect using their precise definitions.' },
  { type: 'must', text: 'I can say why an immutable binding cannot secretly change later in the same scope, and why rebinding a name in GHCi is not the same as mutating it.' },
  { type: 'must', text: 'I can identify the pure centre and the effectful boundary in a short program description.' },
  { type: 'avoid', text: 'Do not describe an expression as an action just because GHCi happens to display its value when you enter it.' },
  { type: 'note', text: 'Real programs still need effects; Haskell keeps them visible at the boundary instead of hiding them inside otherwise-pure code.' }
]} />

Continue with [Functional Thinking](/docs/sem3/csi243/01-functional-thinking/notes/functional-thinking).
